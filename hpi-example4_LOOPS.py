# For Loops
"""for <variable> in (#, #, #, #, #, #,):
	<action-to-loop>
   for <variable> in range(#):
	<action-to-loop>"""

# For loop with intigers
for numb in (0, 1, 2, 3, 4, 5):
	print(numb)

# For loop with strings
for fruit in ("banana", "peach", "melon", "apple", "strawberry")
	print(fruit)

# While loops
"""while <variable-argument>:
	<action-to-loop>"""

switch-level=50
while  >= 100:
	print("Level is fine")
	switch-level = switch-level + 1
